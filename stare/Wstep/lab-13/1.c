#include<stdio.h>
#include<string.h>
#define L1 11
#define L2 17
#define LI 10000 

int por(int i, int j){
 int wyn;
 wym=stremp(b[i],b[j]);
 if(wym == 0) return stremp[a[i], a[j];
  else return wym;
}

main(){
char a[LI][L1], b[LI][L2];
int c[LI], d, f;
for(d=0; d<LI; d++){
 scanf("%s", a[d]);
 scanf("%s", b[d]);
 scanf("%i", &c[d]);
}

for(f=0; f<LI; f++){
 for(d=0; d<L2; d++){
 
    
  }
 }
}

for(f=0; f<LI; f++){
 for(d=0; d<L1; d++){
  printf("%c", a[f][d]);
 }
 printf(" ");
 for(d=0; d<L2; d++){
  printf("%c", b[f][d]);
 }
 printf(" ");
 printf("%i\n", c[f]);
}
printf("\n");
}

